import * as selectors from './albums.selectors';
import reducer from './albums.reducer';
import * as actions from './albums.actions';
import * as sagas from './albums.sagas';

export { reducer, selectors, actions, sagas };
